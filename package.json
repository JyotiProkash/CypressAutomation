{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean:reports": "rmdir /S /Q cypress\\reports && rmdir /S /Q cypress\\screenshots && rmdir /S /Q cypress\\videos && mkdir cypress\\reports\\mocha && mkdir cypress\\reports\\mochareports && mkdir cypress\\screenshots && mkdir cypress\\videos",
    "pretest": "npm run clean:reports",
    "scripts": "cypress run --headed --spec cypress/integration/cypressautomation/checkboxselect.js --browser chrome",
    "combine-reports": "mochawesome-merge cypress/reports/mocha/*.json > cypress/reports/mochareports/report.json",
    "generate-report": "marge cypress/reports/mochareports/report.json -f report -o cypress/reports/mochareports",
    "posttest": "npm run combine-reports && npm run generate-report",
    "test": "npm run scripts || npm run posttest"
  },
  "author": "Jyoti",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.4",
    "cypress": "^8.3.0",
    "cypress-multi-reporters": "^1.5.0",
    "mocha": "^9.0.3",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^5.2.0"
  },
  "dependencies": {}
}
